---
phase: 01-policy-foundation-and-threat-boundaries
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - code_review_graph/security/audit.py
  - code_review_graph/cli.py
  - tests/test_policy_audit.py
  - tests/test_policy_verify.py
autonomous: true
requirements:
  - REQ-06
  - REQ-07
must_haves:
  truths:
    - "Security-relevant policy actions create local audit traces."
    - "Operators can run a command that proves hardened policy status and compliance."
    - "Verification output clearly indicates pass/fail state for local-only policy boundaries."
  artifacts:
    - path: "code_review_graph/security/audit.py"
      provides: "Structured local audit event writer"
    - path: "code_review_graph/cli.py"
      provides: "Policy verification command/output contract"
    - path: "tests/test_policy_verify.py"
      provides: "Verification command contract tests"
  key_links:
    - from: "code_review_graph/cli.py"
      to: "code_review_graph/security/policy_loader.py"
      via: "verify command policy inspection"
      pattern: "verify.*policy"
    - from: "code_review_graph/security/audit.py"
      to: "code_review_graph/security/egress_guard.py"
      via: "policy decision event emission"
      pattern: "policy_(allow|deny|verify)"
---

<objective>
Deliver local audit baseline and operator-facing policy verification for hardened-local enforcement.

Purpose: Make Phase 1 controls observable and provable for operators and test automation.
Output: Structured audit emitter, CLI verification command, and contract tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-policy-foundation-and-threat-boundaries/01-01-PLAN.md
@.planning/phases/01-policy-foundation-and-threat-boundaries/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement local policy audit event baseline</name>
  <files>code_review_graph/security/audit.py, tests/test_policy_audit.py</files>
  <read_first>
    - code_review_graph/security/policy_loader.py
    - code_review_graph/security/egress_guard.py
    - .planning/research/LOCAL_SECURITY_FOUNDATIONS.md
  </read_first>
  <action>Implement local structured audit emission for policy events (load success/failure, allow, deny, verify) with mandatory fields (`timestamp`, `event_type`, `operation`, `result`, `reason`) and metadata-only payloads. Ensure source content is not logged and event sink is local file-based baseline to satisfy REQ-06.</action>
  <verify>
    <automated>uv run pytest tests/test_policy_audit.py -q</automated>
  </verify>
  <acceptance_criteria>Audit events are emitted locally with required schema and no sensitive payload leakage.</acceptance_criteria>
  <done>Audit module exists and tests prove schema presence plus redaction behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI policy verification report</name>
  <files>code_review_graph/cli.py, tests/test_policy_verify.py</files>
  <read_first>
    - code_review_graph/cli.py
    - code_review_graph/security/policy_loader.py
    - code_review_graph/security/egress_guard.py
  </read_first>
  <action>Add a CLI command (or subcommand under existing security/status flow) that prints machine-readable and human-readable verification output showing active profile, egress defaults, guard status, and compliance result. Command must fail non-zero when hardened policy is inactive/misconfigured to satisfy REQ-07 and operational verification in roadmap success criteria.</action>
  <verify>
    <automated>uv run pytest tests/test_policy_verify.py -q</automated>
  </verify>
  <acceptance_criteria>Command reports effective policy state and returns deterministic pass/fail status suitable for automation gates.</acceptance_criteria>
  <done>Policy verification command is implemented with contract tests for success and failure modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy decisions→audit sink | Security outcomes cross into persistent evidence |
| operator command→verification output | Human/automation trust depends on truthful status reporting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | R | audit event storage | mitigate | Structured append-only local events with required fields and reason codes |
| T-01-08 | I | audit payload content | mitigate | Metadata-only audit serialization with redaction checks in tests |
| T-01-09 | T | verification command output | mitigate | Exit-code-enforced pass/fail contract tied to effective loaded policy |
</threat_model>

<verification>
- `uv run pytest tests/test_policy_audit.py tests/test_policy_verify.py -q`
- `uv run ruff check code_review_graph/security code_review_graph/cli.py tests/test_policy_*.py`
</verification>

<success_criteria>
- Local audit baseline exists for policy-relevant actions.
- Operators can verify hardened-local policy status via CLI output and exit code.
- REQ-06 and REQ-07 are covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-policy-foundation-and-threat-boundaries/01-03-SUMMARY.md`
</output>
