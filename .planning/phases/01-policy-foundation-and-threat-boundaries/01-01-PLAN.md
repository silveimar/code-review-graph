---
phase: 01-policy-foundation-and-threat-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code_review_graph/security/policy_schema.py
  - code_review_graph/security/policy_loader.py
  - code_review_graph/security/__init__.py
  - tests/test_policy_schema.py
autonomous: true
requirements:
  - REQ-01
  - REQ-02
must_haves:
  truths:
    - "Hardened local profile exists and is selectable at runtime."
    - "Policy parsing fails closed when config is missing or invalid."
    - "Offline-safe defaults are applied without cloud dependencies."
  artifacts:
    - path: "code_review_graph/security/policy_schema.py"
      provides: "Strict hardened policy schema and defaults"
    - path: "code_review_graph/security/policy_loader.py"
      provides: "Policy loading/validation with fail-closed behavior"
    - path: "tests/test_policy_schema.py"
      provides: "Schema and fail-closed regression coverage"
  key_links:
    - from: "code_review_graph/security/policy_loader.py"
      to: "code_review_graph/security/policy_schema.py"
      via: "validated model construction"
      pattern: "HardenedPolicy\\("
    - from: "tests/test_policy_schema.py"
      to: "code_review_graph/security/policy_loader.py"
      via: "loader contract tests"
      pattern: "load_policy"
---

<objective>
Create the hardened-local policy contract and fail-closed loader used by all subsequent enforcement work.

Purpose: Establish a single trusted policy source before any egress checks are implemented.
Output: Security schema module, loader module, and policy validation tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/LOCAL_SECURITY_FOUNDATIONS.md
@.planning/phases/01-policy-foundation-and-threat-boundaries/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define hardened-local policy schema</name>
  <files>code_review_graph/security/policy_schema.py, code_review_graph/security/__init__.py, tests/test_policy_schema.py</files>
  <read_first>
    - .planning/phases/01-policy-foundation-and-threat-boundaries/01-RESEARCH.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Create strict policy models for hardened-local enforcement per REQ-01/REQ-02 using explicit fields for mode, egress defaults, allowed local destinations, protected data classes (`source_snippet`, `symbol_context`, `full_file_content`, `embedding_input`), and audit settings. Enforce fail-closed schema behavior (unknown keys rejected, invalid types rejected), and export schema types through security package init for downstream usage.</action>
  <verify>
    <automated>uv run pytest tests/test_policy_schema.py -q</automated>
  </verify>
  <acceptance_criteria>Schema rejects unknown/invalid fields, has hardened-local deny-by-default semantics, includes explicit protected data classes used by offline workflow checks, and exports stable types for downstream modules.</acceptance_criteria>
  <done>Typed schema module exists with tests proving strict validation and hardened defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-closed policy loader contract</name>
  <files>code_review_graph/security/policy_loader.py, tests/test_policy_schema.py</files>
  <read_first>
    - code_review_graph/security/policy_schema.py
    - .planning/research/LOCAL_SECURITY_FOUNDATIONS.md
  </read_first>
  <action>Implement policy loading and normalization entry points that return validated policy objects or raise explicit policy errors on ambiguity/malformed config, never permissive fallback. Include profile selection behavior for hardened-local and standard profiles so offline-capable defaults are deterministic (REQ-02), and ensure default hardened policy does not require cloud credentials/providers for build/update/query/review-context flows.</action>
  <verify>
    <automated>uv run pytest tests/test_policy_schema.py -q</automated>
  </verify>
  <acceptance_criteria>Loader returns deterministic policy for valid config, fails closed for missing/invalid policy sources without silently enabling egress, and preserves offline defaults for core workflows without cloud provider requirements.</acceptance_criteria>
  <done>Policy loader API is implemented with test-backed fail-closed behavior and profile selection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file→policy loader | Untrusted policy input crosses into enforcement decisions |
| CLI flags→profile resolver | Runtime selection can alter security posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `policy_loader.py` | mitigate | Strict schema validation and fail-closed exceptions for malformed config |
| T-01-02 | E | profile selection path | mitigate | Hardened-local explicit mode and deny-by-default defaults |
| T-01-03 | R | policy initialization events | mitigate | Structured policy load error signals consumed by audit layer in later plans |
</threat_model>

<verification>
- `uv run pytest tests/test_policy_schema.py -q`
- `uv run ruff check code_review_graph/security tests/test_policy_schema.py`
</verification>

<success_criteria>
- Hardened-local policy contract is explicit, strict, and reusable.
- Invalid policy state cannot produce permissive execution.
- Downstream plans can import policy loader/schema directly without additional design decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-policy-foundation-and-threat-boundaries/01-01-SUMMARY.md`
</output>
