---
phase: 01-policy-foundation-and-threat-boundaries
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - code_review_graph/security/egress_guard.py
  - code_review_graph/embeddings.py
  - code_review_graph/cli.py
  - tests/test_egress_guard.py
  - tests/test_embeddings.py
  - tests/test_main.py
  - tests/test_tools.py
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-06
must_haves:
  truths:
    - "Egress decisions are enforced centrally before outbound-capable operations."
    - "Hardened local mode blocks cloud egress and fails closed on undecidable policy."
    - "Policy-relevant allow/deny actions are test-covered and ready for audit emission."
  artifacts:
    - path: "code_review_graph/security/egress_guard.py"
      provides: "Single allow/deny decision API"
    - path: "code_review_graph/embeddings.py"
      provides: "Integration of centralized guard on embedding provider paths"
    - path: "tests/test_egress_guard.py"
      provides: "Matrix tests for hardened mode egress decisions"
  key_links:
    - from: "code_review_graph/embeddings.py"
      to: "code_review_graph/security/egress_guard.py"
      via: "pre-flight egress check"
      pattern: "check_egress"
    - from: "tests/test_embeddings.py"
      to: "code_review_graph/embeddings.py"
      via: "deny-by-default behavior tests"
      pattern: "PermissionError|deny"
---

<objective>
Implement centralized egress guard enforcement and wire it into outbound integration surfaces.

Purpose: Ensure hardened-local profile has real fail-closed enforcement, not warning-only behavior.
Output: Reusable egress guard plus integration and regression tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-policy-foundation-and-threat-boundaries/01-01-PLAN.md
@.planning/phases/01-policy-foundation-and-threat-boundaries/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build centralized egress guard module</name>
  <files>code_review_graph/security/egress_guard.py, tests/test_egress_guard.py</files>
  <read_first>
    - code_review_graph/security/policy_schema.py
    - code_review_graph/security/policy_loader.py
    - .planning/phases/01-policy-foundation-and-threat-boundaries/01-RESEARCH.md
  </read_first>
  <action>Create a guard API that evaluates operation, destination, and classification against loaded policy and returns explicit allow/deny decisions with machine-readable reasons. Deny by default for hardened-local (REQ-01), including invalid destination parsing and unknown operation classes to satisfy fail-closed behavior.</action>
  <verify>
    <automated>uv run pytest tests/test_egress_guard.py -q</automated>
  </verify>
  <acceptance_criteria>Guard deterministically returns deny for non-localhost cloud destinations in hardened-local and deny on policy ambiguity/error states.</acceptance_criteria>
  <done>Central egress guard exists with matrix coverage of allow/deny/fail-closed paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove offline-capable core workflows under hardened-local policy</name>
  <files>code_review_graph/cli.py, tests/test_main.py, tests/test_tools.py</files>
  <read_first>
    - code_review_graph/cli.py
    - tests/test_main.py
    - tests/test_tools.py
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Add explicit offline policy path tests for core workflows required by REQ-02: build (`code-review-graph build`), update (`code-review-graph update`), query/tool review-context paths (tool entrypoints used for query/review context), confirming they succeed in hardened-local mode without cloud providers configured. Add CLI/runtime guards so optional cloud integrations are never required for these flows.</action>
  <verify>
    <automated>uv run pytest tests/test_main.py tests/test_tools.py -q</automated>
    <automated>uv run pytest tests/test_egress_guard.py tests/test_embeddings.py -q</automated>
  </verify>
  <acceptance_criteria>Tests assert hardened-local profile can execute build/update/query/review-context workflows offline with no cloud credentials, and any attempted cloud egress remains denied.</acceptance_criteria>
  <done>REQ-02 coverage includes explicit, automated offline-core-workflow validation instead of embeddings-only enforcement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce guard in embeddings outbound paths</name>
  <files>code_review_graph/embeddings.py, tests/test_embeddings.py</files>
  <read_first>
    - code_review_graph/embeddings.py
    - code_review_graph/security/egress_guard.py
    - tests/test_embeddings.py
  </read_first>
  <action>Replace warning-only outbound checks with guard-enforced behavior in embedding provider request paths so hardened-local blocks cloud-bound requests and only allows approved local endpoints per policy (REQ-01/REQ-02). Preserve existing non-hardened compatibility while ensuring blocked attempts produce explicit denial signals for downstream audit handling (REQ-06 baseline).</action>
  <verify>
    <automated>uv run pytest tests/test_egress_guard.py tests/test_embeddings.py -q</automated>
  </verify>
  <acceptance_criteria>Cloud egress attempts in hardened-local fail before outbound request; allowed local paths still function; tests assert deny-first semantics.</acceptance_criteria>
  <done>Embeddings integration uses centralized guard and all deny-path regressions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime operation→egress guard | Potential outbound actions must be authorized |
| embeddings provider config→network destination | Provider config can route sensitive content externally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | I | `embeddings.py` outbound calls | mitigate | Mandatory guard check before outbound-capable actions |
| T-01-05 | E | `egress_guard.py` decision fallback | mitigate | Explicit deny on unknown operation/destination or policy errors |
| T-01-06 | R | deny decisions not externally visible | mitigate | Decision object with reason codes consumed by audit and verification plans |
</threat_model>

<verification>
- `uv run pytest tests/test_egress_guard.py tests/test_embeddings.py -q`
- `uv run ruff check code_review_graph/security code_review_graph/embeddings.py tests/test_egress_guard.py`
</verification>

<success_criteria>
- All outbound-capable embedding paths use centralized guard checks.
- Hardened-local profile enforces deny-by-default cloud egress.
- Regression tests cover positive and negative policy outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-policy-foundation-and-threat-boundaries/01-02-SUMMARY.md`
</output>
