---
phase: 04-verification-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_hardening_posture.py
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-05
  - REQ-06
  - REQ-07
must_haves:
  truths:
    - "tests/test_hardening_posture.py exists and includes subprocess verify-policy --json with CRG_SECURITY_PROFILE=hardened_local and at least one in-process guard (egress) reusing security APIs."
    - "Tests are marked with @pytest.mark.hardening_posture for filtering."
  artifacts:
    - path: "tests/test_hardening_posture.py"
      provides: "Orchestrated posture regression surface for Phase 4"
  key_links:
    - from: "tests/test_hardening_posture.py"
      to: "code_review_graph/cli.py"
      via: "subprocess -m code_review_graph verify-policy"
      pattern: "subprocess|json.loads"
---

<objective>
Add **tests/test_hardening_posture.py** — the thin E2E orchestration layer for hardened-local: **subprocess** `python -m code_review_graph verify-policy --json` plus **in-process** checks using `resolve_policy_for_profile` and `check_egress` (same patterns as `tests/test_egress_guard.py`) so REQ-01–REQ-07 are exercised at integration depth without duplicating entire suites.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-verification-and-release-hardening/04-CONTEXT.md
@.planning/phases/04-verification-and-release-hardening/04-RESEARCH.md
@tests/test_policy_verify.py
@tests/test_egress_guard.py
@code_review_graph/security/egress_guard.py
@code_review_graph/security/policy_loader.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Subprocess verify-policy JSON</name>
  <files>tests/test_hardening_posture.py</files>
  <read_first>
    - tests/test_policy_verify.py
    - .planning/phases/04-verification-and-release-hardening/04-CONTEXT.md
  </read_first>
  <action>Create `tests/test_hardening_posture.py` with a class (e.g. `TestHardeningPostureSubprocess`) that uses `subprocess.run` with `sys.executable`, `"-m"`, `"code_review_graph"`, `"verify-policy"`, `"--json"`, `cwd` set to `Path(__file__).resolve().parents[1]`, `env` merging `os.environ` with `CRG_SECURITY_PROFILE=hardened_local`, `CRG_AUDIT_LOG_PATH` pointing to a temp `*.jsonl` under `tmp_path`, and **deleting** `CRG_SECURITY_POLICY_PATH` and `PYTEST_CURRENT_TEST` if present (mirror `TestVerifyPolicySuccess`). Assert returncode 0, parse JSON, assert `compliant is True`, `active_profile` contains `hardened_local`, top-level keys include `egress` and `retention`.</action>
  <verify>
    <automated>uv run pytest tests/test_hardening_posture.py -q --tb=short -k Subprocess</automated>
  </verify>
  <acceptance_criteria>File contains string `verify-policy` and `assert data.get("compliant") is True` (or equivalent); test name includes subprocess/or JSON.</acceptance_criteria>
  <done>Subprocess path proves CLI + REQ-07 JSON contract under hardened profile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: In-process egress fail-closed spot-check</name>
  <files>tests/test_hardening_posture.py</files>
  <read_first>
    - tests/test_egress_guard.py
    - code_review_graph/security/egress_guard.py
  </read_first>
  <action>Add `TestHardeningPostureInProcess` (or similar) with one test that calls `resolve_policy_for_profile("hardened_local", config_path=None)` and `check_egress` with a **denied** destination (e.g. `api.openai.com` / `https` pattern matching existing `test_denies_openai_cloud_host` in test_egress_guard.py). Assert the decision is deny / not allow per existing API.</action>
  <verify>
    <automated>uv run pytest tests/test_hardening_posture.py -q --tb=short -k InProcess</automated>
  </verify>
  <acceptance_criteria>Test references `hardened_local` and `check_egress` and asserts non-allowing outcome for cloud host.</acceptance_criteria>
  <done>In-process path ties orchestration to REQ-01 egress guard without re-running full matrix.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Pytest marker</name>
  <files>tests/test_hardening_posture.py</files>
  <read_first>
    - pyproject.toml
  </read_first>
  <action>Decorate the new test classes or module with `@pytest.mark.hardening_posture` (if pyproject does not yet register the marker, plan 04-02 will add it — at minimum apply decorator here).</action>
  <verify>
    <automated>uv run pytest tests/test_hardening_posture.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>`grep -r "hardening_posture" tests/test_hardening_posture.py` returns matches.</acceptance_criteria>
  <done>Marker applied for downstream CI/docs filtering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Tests run in CI/dev as low-privilege users; subprocess invokes **same repo** interpreter — no extension of trust boundary beyond existing CLI tests.

## Assumptions

- `sys.executable` resolves to an env where `code_review_graph` package is importable (matches CI `pip install -e`).
</threat_model>
