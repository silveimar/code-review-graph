---
phase: 04-verification-and-release-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - pyproject.toml
  - docs/ci-security-regression.md
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-05
  - REQ-06
  - REQ-07
must_haves:
  truths:
    - "pyproject.toml registers pytest marker hardening_posture with description."
    - "docs/ci-security-regression.md explains that CI pytest matrix runs security/hardening tests including test_hardening_posture.py."
  artifacts:
    - path: "docs/ci-security-regression.md"
      provides: "Operator-facing CI regression narrative"
  key_links:
    - from: "docs/ci-security-regression.md"
      to: ".github/workflows/ci.yml"
      via: "documents jobs"
      pattern: "CI"
---

<objective>
Register the **`hardening_posture`** pytest marker and document how **security regressions** are enforced through the existing GitHub Actions **test matrix** (no duplicate pytest invocations per 04-CONTEXT D-05).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-verification-and-release-hardening/04-CONTEXT.md
@.github/workflows/ci.yml
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register pytest marker</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml
  </read_first>
  <action>Under `[tool.pytest.ini_options]`, add `markers =` assignment listing `hardening_posture: ...` with a one-line description of Phase 4 orchestration tests. If `markers` already exists, append without breaking existing keys.</action>
  <verify>
    <automated>uv run pytest --markers 2>&1 | grep -q hardening_posture</automated>
  </verify>
  <acceptance_criteria>`grep -A3 'markers' pyproject.toml | grep -q hardening_posture`</acceptance_criteria>
  <done>Developers can run `pytest -m hardening_posture` locally.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CI security regression doc</name>
  <files>docs/ci-security-regression.md</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Create `docs/ci-security-regression.md` with sections: (1) which CI jobs run on PR (`lint`, `type-check`, `security`/`bandit`, `schema-sync`, `test` matrix), (2) that **full pytest** includes `tests/test_hardening_posture.py` and other `tests/test_*` security modules, (3) optional command `uv run pytest -m hardening_posture`, (4) link to `.github/workflows/ci.yml`.</action>
  <verify>
    <automated>test -f docs/ci-security-regression.md && grep -q 'test_hardening_posture' docs/ci-security-regression.md</automated>
  </verify>
  <acceptance_criteria>File exists and contains string `code-review-graph` or `pytest` and `hardening_posture` or `security`.</acceptance_criteria>
  <done>REQ “CI includes dedicated tests for hardened local policy” is documented for operators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Documentation only — no change to enforcement semantics beyond marker registration.

## Assumptions

- CI remains single pytest invocation per matrix cell; doc matches reality.
</threat_model>
