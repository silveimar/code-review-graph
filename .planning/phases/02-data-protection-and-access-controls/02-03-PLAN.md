---
phase: 02-data-protection-and-access-controls
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - code_review_graph/security/audit.py
  - code_review_graph/tools/_common.py
  - code_review_graph/cli.py
  - code_review_graph/memory.py
  - code_review_graph/wiki.py
  - code_review_graph/graph.py
  - tests/test_policy_audit.py
  - tests/test_phase2_audit.py
autonomous: true
requirements:
  - REQ-06
must_haves:
  truths:
    - "Sensitive operations from Phase 2 (encryption gate success/failure, permission application, blocked plaintext persistence) emit structured JSONL audit events when audit is enabled."
    - "Event taxonomy is stable enough for operators to grep or forward logs."
    - "No PII or repository source content in audit metadata fields per existing `_scrub_metadata` philosophy."
  artifacts:
    - path: "code_review_graph/security/audit.py"
      provides: "Optional new event types / helpers without breaking JSONL schema baseline"
    - path: "tests/test_phase2_audit.py"
      provides: "Audit emission contracts for Phase 2 behaviors"
  key_links:
    - from: "code_review_graph/security/artifact_crypto.py"
      to: "code_review_graph/security/audit.py"
      via: "emit on encrypt deny / key failure"
      pattern: "emit_audit_record"
    - from: "code_review_graph/security/fs_permissions.py"
      to: "code_review_graph/security/audit.py"
      via: "emit on chmod result"
      pattern: "emit_audit_record"
---

<objective>
Expand local audit coverage so security-relevant actions introduced or strengthened in Phase 2 (artifact encryption outcomes, permission hardening, blocked writes) produce structured audit records per REQ-06 and D-05.

Purpose: Close the loop between enforcement and observability for local operators.
Output: Emit sites, event type conventions, and tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-protection-and-access-controls/02-CONTEXT.md
@code_review_graph/security/audit.py
@.planning/phases/01-policy-foundation-and-threat-boundaries/01-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Phase 2 audit event taxonomy</name>
  <files>code_review_graph/security/audit.py, tests/test_phase2_audit.py</files>
  <read_first>
    - code_review_graph/security/audit.py
    - tests/test_policy_audit.py
  </read_first>
  <action>Add documented constants or docstring registry for new `event_type` values (e.g. `artifact_encryption`, `filesystem_permissions`) and `operation` strings for: encryption required-but-denied, encryption success path marker (optional low-volume), permission apply success/failure, permission verification mismatch. Keep records compatible with `REQUIRED_FIELDS` and `_scrub_metadata` — extend scrub allowlist only for non-sensitive structured codes (e.g. `event_subtype`, `path_hint` basename only if needed). Implement helper(s) to reduce duplication at call sites. Tests first: assert JSON shape for each new event.</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_audit.py tests/test_policy_audit.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>Tests pin expected event_type/operation/reason combinations for Phase 2 scenarios without embedding secrets.</acceptance_criteria>
  <done>Taxonomy and helpers are covered by automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Emit audit records from encryption and permission paths</name>
  <files>code_review_graph/security/artifact_crypto.py, code_review_graph/security/fs_permissions.py, code_review_graph/graph.py, code_review_graph/memory.py, code_review_graph/wiki.py, tests/test_phase2_audit.py</files>
  <read_first>
    - .planning/phases/02-data-protection-and-access-controls/02-01-PLAN.md
    - .planning/phases/02-data-protection-and-access-controls/02-02-PLAN.md
    - code_review_graph/security/artifact_crypto.py
    - code_review_graph/security/fs_permissions.py
  </read_first>
  <action>Wire `emit_audit_record` calls at fail-closed branches (missing key, decrypt failure, permission denied) and **non-spammy** success markers for permission application if useful for operators. Respect `policy.audit.enabled`. Ensure encryption audit path does not recurse if audit sink itself fails — swallow logging only as existing audit.py does on OSError.</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_audit.py tests/test_artifact_encryption.py tests/test_fs_permissions.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>Each critical Phase 2 denial path produces at least one audit line in tests using temp audit paths (`CRG_AUDIT_LOG_PATH`).</acceptance_criteria>
  <done>Audit sink reflects Phase 2 security events deterministically in tests.</done>
</task>

<task type="auto">
  <name>Task 3: CLI and MCP-adjacent entry consistency</name>
  <files>code_review_graph/cli.py, code_review_graph/tools/_common.py, tests/test_phase2_audit.py</files>
  <read_first>
    - code_review_graph/cli.py
    - code_review_graph/tools/_common.py
  </read_first>
  <action>Ensure top-level CLI paths that trigger graph build, wiki, or memory operations surface audit emission consistently with MCP tools (same internal helpers). Add integration tests that invoke one CLI command with hardened profile + temp dirs and assert audit file contains expected record for a denied encryption scenario. Avoid duplicate emissions per single logical failure.</action>
  <verify>
    <automated>uv run pytest tests/test_phase2_audit.py tests/test_main.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>CLI and shared tool paths behave consistently for REQ-06 traces in the exercised scenario.</acceptance_criteria>
  <done>REQ-06 trace coverage spans CLI/tool entry points relevant to Phase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit JSONL→operator | Local file may be copied; content must stay metadata-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | I | audit metadata | mitigate | Reuse `_scrub_metadata`; no keys, paths limited to basename/hints |
| T-02-09 | D | audit log tampering | accept | Local JSONL append-only trust model unchanged from Phase 1 |
| T-02-10 | E | missing audit on deny | mitigate | Tests assert emission on deny paths for encryption/permissions |
</threat_model>

<verification>
- `uv run pytest tests/test_phase2_audit.py tests/test_policy_audit.py -q`
- `uv run ruff check code_review_graph/security code_review_graph/cli.py code_review_graph/tools/_common.py`
</verification>

<success_criteria>
- REQ-06 satisfied for Phase 2 security actions with grep-friendly audit events.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-protection-and-access-controls/02-03-SUMMARY.md`
</output>
