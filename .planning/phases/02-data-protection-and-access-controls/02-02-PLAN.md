---
phase: 02-data-protection-and-access-controls
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - code_review_graph/incremental.py
  - code_review_graph/security/fs_permissions.py
  - code_review_graph/cli.py
  - code_review_graph/tools/_common.py
  - tests/test_fs_permissions.py
autonomous: true
requirements:
  - REQ-04
must_haves:
  truths:
    - "Under hardened_local, creating or touching `.code-review-graph/` applies restrictive POSIX permissions where supported (D-01 baseline complement)."
    - "Operators can observe permission posture via existing or extended verification surface without interactive prompts."
    - "Path validation continues to use `_validate_repo_root` and related guards — no broadened trust boundary."
  artifacts:
    - path: "code_review_graph/security/fs_permissions.py"
      provides: "Optional hardened directory/file mode application"
    - path: "tests/test_fs_permissions.py"
      provides: "Cross-platform behavior and chmod expectations"
  key_links:
    - from: "code_review_graph/incremental.py"
      to: "code_review_graph/security/fs_permissions.py"
      via: "post-create permission hardening"
      pattern: "get_data_dir|apply_hardened"
    - from: "code_review_graph/cli.py"
      to: "code_review_graph/security/fs_permissions.py"
      via: "verify-policy or dedicated check reporting modes"
      pattern: "verify.policy|fs_permissions"
---

<objective>
Enforce strict local filesystem access defaults on generated data and runtime artifacts under the repo data directory, aligned with REQ-04 and D-04 (implementation discretion on masks and CLI details).

Purpose: Restrict unauthorized local processes from reading or tampering with graph and derived artifacts on Unix-like systems; document Windows limitations.
Output: Permission helper module, integration at data-dir creation, verification/report hooks, tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-protection-and-access-controls/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@code_review_graph/tools/_common.py
@code_review_graph/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fs_permissions helper</name>
  <files>code_review_graph/security/fs_permissions.py, tests/test_fs_permissions.py</files>
  <read_first>
    - code_review_graph/incremental.py
    - .planning/codebase/CONCERNS.md
  </read_first>
  <action>Implement `apply_hardened_data_dir_permissions(data_dir: Path) -> None` (or equivalent) that sets directory mode `0o700` and default file mode `0o600` for files directly under `.code-review-graph/` where `os.chmod` applies; no-op or guarded behavior on Windows with explicit documentation in docstrings. Must never chmod outside resolved `data_dir`. Add tests: Unix asserts modes; Windows skips or checks no crash.</action>
  <verify>
    <automated>uv run pytest tests/test_fs_permissions.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>Helper is safe on boundary paths, testable on POSIX, and documented for platform variance per REQ-04.</acceptance_criteria>
  <done>Module exists with passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate with get_data_dir and repo validation</name>
  <files>code_review_graph/incremental.py, code_review_graph/tools/_common.py</files>
  <read_first>
    - code_review_graph/security/policy_loader.py
    - code_review_graph/incremental.py
  </read_first>
  <action>After `get_data_dir` creates the tree, invoke permission hardening when active security profile is `hardened_local` (reuse the same profile resolution pattern as CLI — env `CRG_SECURITY_PROFILE` and/or policy load without circular imports; extract tiny helper if needed). Ensure `_validate_repo_root` invariants remain unchanged. Avoid double-chmod on every read — hook at creation is sufficient.</action>
  <verify>
    <automated>uv run pytest tests/test_fs_permissions.py tests/test_incremental.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>Hardened profile triggers chmod exactly where intended; standard profile does not break developer ergonomics.</acceptance_criteria>
  <done>Data dir creation applies permissions under hardened_local only.</done>
</task>

<task type="auto">
  <name>Task 3: Expose permission status in verification CLI</name>
  <files>code_review_graph/cli.py, tests/test_fs_permissions.py</files>
  <read_first>
    - code_review_graph/cli.py
    - .planning/phases/01-policy-foundation-and-threat-boundaries/01-03-PLAN.md
  </read_first>
  <action>Extend `verify-policy` (or add a sub-flag) to report whether data-dir permissions match hardened expectations: print OK/WARN with octal modes when readable; exit non-zero on critical mismatch only under hardened_local if feasible without breaking CI on exotic FS. Keep output machine-parse friendly (stable keywords/lines). Wire tests through subprocess or CLI test helpers consistent with `tests/test_policy_verify.py` patterns.</action>
  <verify>
    <automated>uv run pytest tests/test_fs_permissions.py tests/test_policy_verify.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>Operators can see permission verification status deterministically; documented in plan SUMMARY for runbook use.</acceptance_criteria>
  <done>CLI reflects REQ-04 verification path alongside Phase 1 verify-policy contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local FS→process | Other local users/processes may attempt to read `.code-review-graph/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | I | data dir world-readable | mitigate | 0700/0600 modes on POSIX per helper |
| T-02-06 | E | chmod on wrong path | mitigate | Operate only under resolved `get_data_dir` path; validate repo_root precedes |
| T-02-07 | I | Windows ACL gap | accept | Document limitation; no false guarantee of Unix-equivalent ACLs in v1 |
</threat_model>

<verification>
- `uv run pytest tests/test_fs_permissions.py tests/test_incremental.py tests/test_policy_verify.py -q`
- `uv run ruff check code_review_graph/incremental.py code_review_graph/security/fs_permissions.py code_review_graph/cli.py`
</verification>

<success_criteria>
- REQ-04 enforced via permission defaults and observable verification for hardened_local.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-protection-and-access-controls/02-02-SUMMARY.md`
</output>
