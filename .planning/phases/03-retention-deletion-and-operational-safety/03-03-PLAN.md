---
phase: 03-retention-deletion-and-operational-safety
plan: 03
type: execute
wave: 1
depends_on:
  - 03-02-PLAN.md
files_modified:
  - code_review_graph/cli.py
  - docs/ (or README security section — match repo doc layout)
  - tests/test_policy_verify.py
autonomous: true
requirements:
  - REQ-05
  - REQ-06
must_haves:
  truths:
    - "`verify-policy` JSON/human output includes a retention section summarizing configured limits and next-step pointers (non-destructive)."
    - "Operator runbook documents cleanup verification steps and residual-risk caveats (SSD/FDE) per CONTEXT D-03."
  artifacts:
    - path: "docs/"
      provides: "Operational safety runbook markdown"
    - path: "tests/test_policy_verify.py"
      provides: "Extended assertions for retention block in verify-policy output"
  key_links:
    - from: "code_review_graph/cli.py"
      to: "code_review_graph/security/policy_schema.py"
      via: "verify-policy reads retention for summary"
      pattern: "verify.policy|verify-policy"
---

<objective>
Publish **runbook-grade guidance** and wire **verify-policy** to surface retention configuration and cleanup affordances without executing destructive operations.

Purpose: REQ-05/REQ-06 operational clarity.
Output: Documentation + verify-policy extensions + tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-retention-deletion-and-operational-safety/03-CONTEXT.md
@code_review_graph/cli.py
@tests/test_policy_verify.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verify-policy retention section</name>
  <files>code_review_graph/cli.py, tests/test_policy_verify.py</files>
  <read_first>
    - tests/test_policy_verify.py
    - code_review_graph/cli.py
  </read_first>
  <action>Extend `verify-policy` output (table + `--json`) with a **Retention** section: active max_age_days per sink, note if unlimited, reference cleanup subcommand from 03-02. No deletion here.</action>
  <verify>
    <automated>uv run pytest tests/test_policy_verify.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>JSON schema stable — additive keys only.</acceptance_criteria>
  <done>Verification surfaces retention posture.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 2: Operational runbook markdown</name>
  <files>docs/security-retention.md (exact path follows repo conventions)</files>
  <read_first>
    - CLAUDE.md or existing docs folder structure
  </read_first>
  <action>Add concise runbook: how to check retention config, run dry-run cleanup, interpret audit entries, and understand deletion residual risks. Link from main README or security docs index if one exists.</action>
  <verify>
    <automated>uv run pytest tests/ -q --tb=short -k policy_verify</automated>
  </verify>
  <acceptance_criteria>Doc paths referenced in verify-policy help or README cross-link.</acceptance_criteria>
  <done>Runbook discoverable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Docs must not encourage disabling encryption or policy to “speed up” cleanup.

## Assumptions

- English docs for this repo’s primary audience.
</threat_model>
