---
phase: 03-retention-deletion-and-operational-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - code_review_graph/security/policy_schema.py
  - code_review_graph/security/__init__.py
  - code_review_graph/security/policy_loader.py
  - tests/test_retention_policy.py
autonomous: true
requirements:
  - REQ-05
must_haves:
  truths:
    - "Retention limits are expressible in hardened policy as optional max_age_days per logical artifact family (audit, memory, wiki, graph metadata), nullable meaning unlimited."
    - "Policy validates strictly with extra=forbid; defaults preserve existing installs."
  artifacts:
    - path: "code_review_graph/security/policy_schema.py"
      provides: "RetentionPolicy (or equivalent) nested under HardenedPolicy"
    - path: "tests/test_retention_policy.py"
      provides: "Schema defaults and validation coverage"
  key_links:
    - from: "code_review_graph/security/policy_loader.py"
      to: "code_review_graph/security/policy_schema.py"
      via: "loads retention fields"
      pattern: "HardenedPolicy|Retention"
---

<objective>
Add a **declarative retention configuration** to the hardened security profile so REQ-05 time-primary limits (from Phase 3 CONTEXT) can be enforced in downstream evaluation and CLI plans.

Purpose: Schema + loader only — no destructive behavior in this plan.
Output: Extended `policy_schema`, loader compatibility, tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-retention-deletion-and-operational-safety/03-CONTEXT.md
@.planning/phases/03-retention-deletion-and-operational-safety/03-RESEARCH.md
@code_review_graph/security/policy_schema.py
@code_review_graph/security/policy_loader.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RetentionPolicy sub-model</name>
  <files>code_review_graph/security/policy_schema.py, code_review_graph/security/__init__.py, tests/test_retention_policy.py</files>
  <read_first>
    - code_review_graph/security/policy_schema.py
    - .planning/phases/03-retention-deletion-and-operational-safety/03-CONTEXT.md
  </read_first>
  <action>Add optional integer fields (nullable `Optional[int]` or sentinel pattern per codebase convention) for **max_age_days** scoped at minimum to: `audit_log`, `memory_artifacts`, `wiki_outputs`, `graph_derived` — naming aligned with existing audit/memory/wiki paths. Document semantics: age computed from file mtime or logical record timestamp where applicable (evaluation deferred to 03-02). Attach `retention: RetentionPolicy` to `HardenedPolicy` with `Field(default_factory=RetentionPolicy)`. Export types.</action>
  <verify>
    <automated>uv run pytest tests/test_retention_policy.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>Tests prove serialization round-trip and forbid-unknown-keys behavior.</acceptance_criteria>
  <done>Retention schema merged with defaults backward-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Loader and fixtures</name>
  <files>code_review_graph/security/policy_loader.py, tests/test_retention_policy.py</files>
  <read_first>
    - code_review_graph/security/policy_loader.py
  </read_first>
  <action>Ensure sample hardened YAML fixtures (if used in tests) accept new optional `retention` block without migration breakage.</action>
  <verify>
    <automated>uv run pytest tests/test_retention_policy.py tests/ -q --tb=short -k retention --co -q 2>/dev/null | head -5; uv run pytest tests/test_retention_policy.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>Loading policies without retention block yields sane defaults.</acceptance_criteria>
  <done>Loader accepts retention configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- **Attacker with local user access** can delete files; retention policy is **not** a crypto boundary — it limits **residual data lifetime** for normal operation.
- **Misconfiguration** could delete wanted data; mitigations live in 03-02 (dry-run, explicit apply).

## Assumptions

- Single-user local CLI; no multi-tenant scheduling in this plan.
</threat_model>
