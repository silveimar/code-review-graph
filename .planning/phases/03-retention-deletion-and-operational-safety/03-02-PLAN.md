---
phase: 03-retention-deletion-and-operational-safety
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01-PLAN.md
files_modified:
  - code_review_graph/cli.py
  - code_review_graph/security/audit.py
  - code_review_graph/memory.py
  - code_review_graph/incremental.py
  - tests/test_retention_cleanup.py
autonomous: true
requirements:
  - REQ-05
  - REQ-06
must_haves:
  truths:
    - "Cleanup command supports --dry-run (default or explicitly safe) listing candidates without destructive unlink unless user confirms apply semantics per CONTEXT D-02."
    - "Deletion uses pragmatic unlink/SQL delete + document residuals per CONTEXT D-03; audit events emitted for cleanup actions (REQ-06)."
  artifacts:
    - path: "code_review_graph/cli.py"
      provides: "cleanup/prune subcommand wiring"
    - path: "tests/test_retention_cleanup.py"
      provides: "Dry-run and bounded destructive tests in tmp paths"
  key_links:
    - from: "code_review_graph/cli.py"
      to: "code_review_graph/memory.py"
      via: "invokes cleanup helpers"
      pattern: "cleanup|prune|memory"
---

<objective>
Implement **explicit operator-driven cleanup**: evaluate retention policy against on-disk artifacts under `.code-review-graph/`, preview via dry-run, apply deletes with audit trails.

Purpose: REQ-05 enforcement entry points; REQ-06 audit for cleanup.
Output: CLI surface + helpers + tests using isolated temp repo dirs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-retention-deletion-and-operational-safety/03-CONTEXT.md
@.planning/phases/03-retention-deletion-and-operational-safety/03-RESEARCH.md
@code_review_graph/cli.py
@code_review_graph/security/audit.py
@code_review_graph/graph.py
@code_review_graph/memory.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retention evaluation module (pure)</name>
  <files>code_review_graph/security/retention_eval.py (or equivalent), tests/test_retention_cleanup.py</files>
  <read_first>
    - .planning/phases/03-retention-deletion-and-operational-safety/03-RESEARCH.md
    - code_review_graph/security/policy_schema.py
  </read_first>
  <action>Implement a small module that, given `HardenedPolicy` + resolved data directory, returns a structured list of **cleanup candidates** (path, reason, age_days) per sink — **no side effects**. Respect CONTEXT: graph DB handling may be truncate tables vs delete file depending on open handles — document chosen approach.</action>
  <verify>
    <automated>uv run pytest tests/test_retention_cleanup.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>Unit tests use tmp_path fixtures with fake-old files.</acceptance_criteria>
  <done>Pure evaluation tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI cleanup command</name>
  <files>code_review_graph/cli.py, tests/test_retention_cleanup.py</files>
  <read_first>
    - code_review_graph/cli.py
  </read_first>
  <action>Add subcommand (name final in implementation; e.g. `cleanup-data`) with `--dry-run` as the safe default **or** require `--apply` to mutate — pick one pattern and document in help string. Wire to policy loader and evaluation; on `--apply`, perform deletes, VACUUM where needed for SQLite, emit audit records via `emit_audit_record` for each batch or summary per project logging discipline.</action>
  <verify>
    <automated>uv run pytest tests/test_retention_cleanup.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>No deletion without explicit apply flag; audit sink receives cleanup events in tests (mock or tmp audit path).</acceptance_criteria>
  <done>CLI cleanup operational under tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Cleanup must respect `_validate_repo_root` / data dir resolution consistent with existing CLI commands.

## Assumptions

- Operators read dry-run output before `--apply`.
</threat_model>
